# Secrets — never commit these
.env

# Python virtual environment
.venv/
__pycache__/
*.pyc
*.pyo

# Runtime state — generated at runtime, not part of source
.alfred.pid
.reminder_state.json
memory.db
